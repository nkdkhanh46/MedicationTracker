<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:app="http://schemas.android.com/apk/res-auto">
    <androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/topLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:keepScreenOn="true">

        <com.martin.medicationtracker.features.addmedication.orccapture.CameraSourcePreview
            android:id="@+id/vPreview"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <com.martin.medicationtracker.features.addmedication.orccapture.GraphicOverlay
                android:id="@+id/vGraphicOverlay"
                android:layout_width="match_parent"
                android:layout_height="match_parent" />
        </com.martin.medicationtracker.features.addmedication.orccapture.CameraSourcePreview>

        <TextView
            android:id="@+id/btnSkip"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/spacing_normal"
            android:padding="@dimen/spacing_normal"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:background="@color/colorPrimary"
            android:textColor="@color/colorWhite"
            android:text="@string/ocr_skip"/>
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
